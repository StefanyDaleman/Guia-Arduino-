int Led1 = 12;
int Led2 = 8;
int Led3 = 4; 
int dt = 1000;

void setup () {
  pinMode(Led1, OUTPUT);
  pinMode(Led2, OUTPUT);
  pinMode(Led3, OUTPUT);
  
}

void loop() {
 digitalWrite(Led1,HIGH);
 digitalWrite(Led2,HIGH);
 digitalWrite(Led3,HIGH);
 delay (dt);
}
